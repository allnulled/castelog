Castelog.metodos.pasado_por = function (parametros, fns) {
    const is_multinfunc = Array.isArray(fns);
    const is_multinparam = Array.isArray(parametros);
    let output = undefined;
    if (is_multifunc) {
        if (is_multiarray) {

        } else {

        }
    } else {
        if (is_multiarray) {

        } else {

        }
    }
};